apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    #cert-manager.io/cluster-issuer: letsencrypt-http01-production
    kubernetes.io/ingress.class: nginx
    # allow upload / post up to 1024 megabytes
    nginx.ingress.kubernetes.io/proxy-body-size: 1024m
    # enforce http to https redirection for authentication
    #nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
  labels:
    app: xplan-services-plu-ingress
  name: xplan-services-plu-ingress
spec:
  rules:
  - host: atlas
    http:
      paths:
        - path: /xplan-inspireplu/services
          pathType: Prefix
          backend:
            service:
              name: xplan-plu-services
              port: 
                number: 8080
